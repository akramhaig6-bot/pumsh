# 🔍 التقرير التشخيصي الشامل — مشروع منصة نَما بعد النشر على Vercel

**التاريخ:** 2026-09-08 — **الفرع:** arena/01a081fe-pumsh — **الالتزام المفحوص:** 4106cfc

---

## الخلاصة التنفيذية (بأدلة، لا تخمين)

البناء على Vercel نجح — وهذا صحيح ومؤكَّد. لكن **الموقع بعد النشر لا يعمل**، والسبب مشكلتان مستقلتان ومؤكدتان، إحداهما Runtime بحتة والثانية معمارية:

### 🔴 المشكلة 1: خطأ Runtime يَكسر الموقع بالكامل (شاشة بيضاء على كل الصفحات)

- **الملف:** `client/src/store.jsx`
- **السطر:** 91 (قيمة الـ Context) مقابل السطرين 98 و 101 (المكوّن `Toasts`)
- **التفاصيل:** المكوّن `AppProvider` يمرّر في `value` الحقول:
  `{ user, meReady, unread, socketOn, toast, setAuth, refreshMe, logout }` — **`toasts` (مصفوفة الإشعارات المنبثقة) غير موجودة في القيمة**، بينما المكوّن `<Toasts />` (المُركَّب دائمًا داخل `AppProvider` في `App.jsx` سطر 64) ينفّذ `const { toasts } = useApp()` ثم `toasts.map(...)`.
- النتيجة: `toasts === undefined` ← `TypeError: Cannot read properties of undefined (reading 'map')` ← React يُسقط الشجرة كاملة (لا يوجد ErrorBoundary في `client/src/App.jsx`) ← **شاشة بيضاء على كل مسار**، حتى قبل أي طلب شبكة.
- **لماذا لم يظهر أثناء `vite build`؟** لأن Vite/esbuild يكتفي بالتحويل والدمج (Static analysis). لا ينفّذ الكود ولا يتحقق من وجود المفاتيح في كائنات Context. "82 modules transformed" تعني أن الوحدات حُوِّلت بنجاح فقط.
- **الإثبات العملي (نفّذته هنا):**
  1. `npm run build` محليًا أعاد نفس الناتج بالضبط: `✓ 82 modules transformed … ✓ built in 2.48s` — تطابق تام مع سجل Vercel.
  2. شغّلت الحزمة المبنية (`dist/assets/index-*.js`) في بيئة DOM فعلية:
     ```
     TypeError: Cannot read properties of undefined (reading 'map')
         at Toasts (index-*.js:18629)
     root children: 0 — body: (فارغ) → شاشة بيضاء
     ```
  3. نفس النتيجة مع نسخة غير مصغّرة (`--minify false`) والـ stack يسمّي `Toasts` صراحة — أي أن الخطأ حقيقي ومستقل عن بيئة الاختبار، وسيظهر في Dev وفي Production وفي أي متصفح.

### 🔴 المشكلة 2: النشر على Vercel لا يشمل الـ Backend أصلًا — بنية المشروع خادم-واحد

- **الملف:** `vercel.json` + `package.json` + `server/index.js`
- المشروع (كما يوضح `README.md` نفسه: "Full-Stack على سيرفر واحد… Express + SQLite + Socket.IO + واجهة الويب") تطبيق **Node يعمل كخادم واحد**: يقدّم الـ API (`/api/*`) ويخدم ملفات `dist` ويشغّل WebSocket (`/socket.io`) ويخزّن قاعدة SQLite وملفات الرفع على قرص محلي (`data/`).
- `vercel.json` الحالي لا يحتوي أي `builds` ولا تعريف Functions — فيه **فقط** `rewrites` (كل طلب → `index.html`) و headers. بالتالي Vercel نشر **ملفات `dist` الثابتة فقط** ولا يوجد أي خادم Express في أي مكان.
- **الإثبات العملي:** بنيت محاكاة مطابقة لسلوك Vercel (ملفات ثابتة + catch-all rewrite) وطلبت المسارات:
  | الطلب | الاستجابة على Vercel (محاكاة) |
  |---|---|
  | `GET /` | `200 text/html` ✅ |
  | `GET /assets/index-*.js` | `200` ✅ |
  | `GET /offers` (تحديث مباشر) | `200 HTML` (index.html) ✅ (الـ SPA fallback يعمل) |
  | `GET /api/auth/csrf` | `200` لكن **`text/html`** ❌ |
  | `GET /api/offers` | `200` لكن **`text/html`** ❌ |
  | `GET /api/up/xxx.jpg` (الصور) | HTML بدل صورة ❌ |
  | `/socket.io/` | HTML ❌ |
- الكود في الواجهة يتعامل بأمان مع هذا (لا انهيار): `api()` يحاول `res.json()` ويفشل بهدوء، لكن **كل بيانات الموقع ووظائفه تعتمد على هذه الـ APIs**: المحتوى، البحث، التسجيل/الدخول، الطلبات، التذاكر، اللوحة، الرفع، الإشعارات الفورية. أي حتى بعد إصلاح المشكلة 1 سيبقى الموقع **قشرة فارغة**: تسجيل الدخول مستحيل (`/api/auth/login` يرد HTML)، وكل قائمة فارغة، وكل صورة مكسورة.
- **لماذا نجح Vercel Build؟** Vercel بنى الواجهة فقط (وكان ذلك سليمًا 100%)، و"Deployment completed" تعني أن **ملفات static نُشرت** — لا يعني أن الخادم شُغّل (لم يُشغَّل إطلاقًا).
- **لماذا لا يمكن ببساطة "تشغيل `npm start` على Vercel"؟** Vercel بيئة Serverless/Stateless: لا يدعم WebSocket دائمًا ولا قاعدة SQLite على قرص دائم ولا ملفات مرفوعة على قرص؛ أي محاولة تشغيل Express هناك ستفشل وظيفيًا (ملفات `/tmp` تُمحى مع كل دالة، ولا جلسات WebSocket).

---

## تفاصيل الفحص (ماذا نفّذت فعليًا)

1. فحص بنية الملفات: `package.json`, `vercel.json`, `vite.config.ts` (الجذر و`client/`), `index.html` (الجذر و`client/`), `tsconfig*`, `postcss/tailwind`, `.env.example`, `.gitignore`.
2. فحص كامل للواجهة: `main.tsx`, `App.jsx`, `store.jsx`, `lib/api.jsx`, وكل الصفحات والمكوّنات (imports/مسارات/صور).
3. فحص كامل للخادم: `config.js`, `index.js`, `db.js`, `routes/*`, `lib/*` — تأكدت أن الخادم سليم ويُقلع.
4. `npm install` ثم `npm run build` → **إعادة إنتاج مطابقة لسجل Vercel** (82 modules / 2.48s).
5. تشغيل الخادم كاملًا (`npm start` بنسخة Node 22 محليًا) مع `dist/`:
   - `/api/health` → `{"ok":true,…}` ✅ — قاعدة SQLite تُنشأ وتعمل
   - `/` → HTML الواجهة ✅ — `/offers`, `/admin/users` → `200` (SPA fallback داخل الخادم يعمل) ✅
   - `/api/offers`, `/api/meta` → JSON سليم ✅
6. محاكاة Vercel Static (انظر الجدول أعلاه) — الفشل محصور في غياب الخادم.
7. **تشغيل حزمة الواجهة المبنية في بيئة DOM** → اكتشاف انهيار `Toasts` (المشكلة 1) وتأكيده بنسختين (مصغّرة وغير مصغّرة).
8. فحص حالة الأمان: كوكيات CSRF/الجلسة، rate limit، تحقق من الإعدادات الافتراضية الخطرة في الإنتاج.
9. فحص البيئة: أي `localhost` أو `VITE_*` أو `import.meta.env` في الواجهة (لا يوجد — كل الطلبات same-origin نسبية)، أي خطوط خارجية (لا يوجد)، أي مرجع لصور غير موجودة (لا يوجد)، Case-sensitivity (البناء نجح على نظام حساس لحالة الأحرف = لا مشكلة).

---

## النتائج مصنّفة حسب بنود طلبك (26 بندًا)

### 🔴 مشاكل مؤكدة تمنع التشغيل

| # | البند | التفاصيل |
|---|---|---|
| 1 | **انهيار React عند أول Render** (بنودك 12, 13, 21, 25) | `client/src/store.jsx:91` — القيمة الممررة من `AppProvider` لا تحتوي `toasts`؛ المكوّن `<Toasts/>` (`store.jsx:98,101`، يُركَّب في `App.jsx:64`) يقرؤها → `TypeError … reading 'map'` → شاشة بيضاء. **سبب عدم ظهوره في البناء:** البناء لا ينفّذ الكود. **الحل:** إضافة `toasts` إلى كائن `value` ومصفوفة الاعتماديات في `useMemo` (سطر 91-92). سطر واحد، بدون تغيير أي وظيفة أو تصميم. |
| 2 | **لا يوجد Backend في النشر** (بنودك 1, 2, 3, 4, 9, 10, 14, 15, 19, 20, 24) | `vercel.json` (لا `builds`/Functions) + طبيعة المشروع خادم-واحد (`server/index.js:97-117`). Vercel نشر static فقط. كل `/api/*` يرد `200 HTML` (بسبب rewrite) أو `404`، و`/socket.io` لا يعمل. حتى مع إصلاح المشكلة 1: واجهة بلا بيانات وبلا مصادقة. **الحل:** نشر الخادم على بيئة Node قادرة على تشغيل عملية دائمة + قرص دائم (انظر خطة الإصلاح، الخيار A هو الموصى به). |

### 🟠 مشاكل محتملة/تحتاج تحقق (تظهر بعد إصلاح المشكلتين أعلاه)

| # | البند | التفاصيل |
|---|---|---|
| 3 | **إعدادات مشروع Vercel غير معروفة** (بند 3) | يجب التحقق في Vercel Dashboard: Framework Preset و Build Command و **Output Directory** و Root Directory. إذا كان Output Directory ≠ `dist`، فـ Vercel يخدم ملفات جذر المستودع — وهناك `index.html` قديم (نموذج أولي يشير إلى `/src/main.tsx`) سيسبب شاشة بيضاء أيضًا. أرسل لي لقطة إعدادات المشروع لتأكيد ذلك. |
| 4 | **بيئة التشغيل Node** (بند 4) | `server/db.js:1` يستخدم `node:sqlite` — يتطلب Node ≥ 22.5 (بدون علم تجريبي: ≥ 22.13). `package.json` **لا يصرّح بـ `engines`** → على مضيف Node قد تُثبَّت نسخة أقدم وينهار الخادم وقت الإقلاع. الحل: إضافة `"engines": { "node": ">=22.13" }`. |
| 5 | **`PUBLIC_URL` غير مضبوط** (بنود 19, 20) | `server/config.js:13` — الافتراضي `http://localhost:8080`. في الإنتاج الحقيقي يُستخدم هذا القيمة في: (أ) روابط إيميلات الاستعادة، (ب) `cors.origin` لـ Socket.IO (`server/index.js`). إن تُرك افتراضيًا على نطاق حقيقي: روابط الاستعادة تشير إلى localhost، و**مصافحة WebSocket يرفضها الخادم بسبب عدم تطابق الـ origin** (إشعارات فورية لا تعمل). يجب ضبطه على `https://domain` في بيئة الإنتاج. |
| 6 | **`SECRET` وبيانات الأدمن الافتراضية** (بند 20) | `server/config.js:14` (`dev-secret-change-me`) و `seed.js` (أدمن أول بكلمة `Admin@12345` إن لم تُضبط `ADMIN_PASSWORD`). على أي مضيف حقيقي هذا ثغرة — يجب ضبط `SECRET` و `ADMIN_EMAIL`/`ADMIN_PASSWORD` قبل أول تشغيل. |
| 7 | **البيانات والرفع على قرص محلي** (بند 24) | `data/` (SQLite + uploads) داخل مجلد المشروع (`server/config.js:21`). أي إعادة نشر/إعادة تشغيل على مضيف بدون قرص دائم (volume) = فقدان كل شيء. يجب تثبيت volume دائم على المسار `data/`. |
| 8 | **`MAIL_MODE=none` افتراضيًا** (بند 24) | `server/config.js:23` — بدون SMTP حقيقي، "نسيت كلمة المرور" تسجّل الرابط في سجل الخادم فقط ولا يصلك بريد. يجب إعداد SMTP للإنتاج (اختياري وظيفيًا إن اكتفيت بإعادة التعيين اليدوي من الأدمن). |

### 🟡 تحذيرات لا تمنع التشغيل

| # | البند | التفاصيل |
|---|---|---|
| 9 | **`esbuild@0.25.12`** (بند 26) | هذه هي نسخة esbuild التي تعتمد عليها `vite@6.4.3` نفسها (`npm ls` يؤكد: `vite → esbuild@0.25.12`)، وهي مطابقة محليًا وعلى Vercel. أعدتُ البناء بهذه النسخة بالضبط بنجاح (82 modules / 2.48s). أي سطر "warning" حولها في سجل Vercel هو إشعار من npm/esbuild أثناء التثبيت و**ليس له أي أثر على التشغيل** — الحزمة الثنائية عملت وبنَت. إن أرسلت النص الحرفي للسطر أؤكده لك حرفيًا. 🟢 فعليًا. |
| 10 | **`multer@1.4.5-lts.2`** | npm يعلنها deprecated (ثغرات معالجة في 1.x) — ترقية إلى multer 2.x مستحسنة أمنيًا، لا علاقة لها بتشغيل الموقع. |
| 11 | **`config.version` غير معرّف** | `server/index.js` (مسار `/api/health`) يقرأ `config.version` غير الموجود في `config.js` → المفتاح `version` يختفي من استجابة الـ health (JSON.stringify يتجاهل undefined). لا يكسر شيئًا؛ يُضاف الحقل أو يُحذف السطر. |
| 12 | **`alert()` لرسائل أخطاء الشبكة** | `client/src/pages/public.jsx` (مثلًا 103, 138, 243) تستخدم `alert(e.message)` عند فشل API — في غياب الخادم سترى نوافذ تنبيه مزعجة. بعد إصلاح الاستضافة لن تظهر؛ استبدالها بـ `toast` تحسين UX اختياري. |
| 13 | **ملفات نموذج أولي قديم في المستودع** | `src/App.tsx`, `src/main.tsx`, `src/index.css`, `index.html` (الجذر), `public/editorial-hero.jpg`, `part 1..10`, `Part6`, `index 2` — متعقّبة في Git لكنها **خارج بناء Vite** (الجذر الحقيقي `client/`) ولا تؤثر على النشر إطلاقًا. تسبب لبسًا فقط (والنموذج القديم يستورد `lucide-react` غير الموجود في dependencies). تنظيف اختياري. |
| 14 | **`npm test` معطوب** | `package.json` يشير إلى `server/test/` وهو غير موجود — أمر تطوير فقط. |
| 15 | **لا يوجد favicon** | المتصفح يطلب `/favicon.ico` فيرد HTML عبر الـ rewrite — غير مؤذٍ. |
| 16 | **`express-rate-limit` و`helmet`** | مهيأة بشكل سليم (`trust proxy: 1`، CSP معطّل عن قصد لـ HMR/WS — مقبول لأن التعقيم يتم في API). |

### 🟢 لا توجد مشكلة (تم التحقق منها)

| # | البند | الدليل |
|---|---|---|
| 17 | **بناء Vite والإخراج** (بند 2, 3, 6, 17) | `client/vite.config.ts` سليم؛ `outDir` → `dist` (أكّدته بفحص ناتج البناء)، assets بمسارات مطلقة `/assets/...` — صحيحة لأن الموقع في جذر النطاق. |
| 18 | **SPA fallback / تحديث صفحة مباشر** (بنود 5, 23) | في **خادم Node الكامل**: `app.get("*")` يخدم index.html (جرّبته: `/offers`, `/admin/users` → 200 HTML). في **Vercel static**: rewrite في `vercel.json` يعمل أيضًا (محاكاة: 200). آلية الـ fallback نفسها ليست المشكلة. |
| 19 | **Case sensitivity** (بند 18) | كل الاستيرادات متطابقة الحالة؛ والبناء تم على Linux (حساس) بنجاح. |
| 20 | **الصور/الخطوط/الموارد** (بند 17) | لا خطوط خارجية، لا @import شبكي؛ `client/src/styles.css` محلي. لا مراجع لصور غير موجودة في الواجهة. (صور المحتوى تُحمَّل من `/api/up/...` — تعمل فقط بوجود الخادم: ضمن المشكلة 2.) |
| 21 | **الاعتماد على localhost في الواجهة** (بند 19) | لا يوجد أي `localhost` أو `VITE_*` في كود الواجهة — كل الطلبات نسبية same-origin. `localhost:8080` موجود فقط في `client/vite.config.ts` (proxy للتطوير — لا يُضمَّن في الناتج) و في `server/config.js` كقيمة افتراضية لـ `PUBLIC_URL` (انظر 🟠 5). |
| 22 | **React/Vite initialization والـ imports** (بنود 8, 11, 22) | `client/src/main.tsx` → `createRoot` سليم، CSS مستوردة، `@` alias معرّف ومستخدم باستمرار، لا imports مفقودة (البناء + تشغيل الحزمة أكدا ذلك؛ الانهيار الوحيد كان خلل `toasts` المذكور). |
| 23 | **CSRF/Cookies design** (بند 15) | `nama_csrf` غير HttpOnly (مقروء من JS كما يفترض `api.jsx`) مع `SameSite=Strict`؛ جلسة `nama_sid` HttpOnly. التصميم سليم **عندما يكون API والواجهة على نفس الأصل** (وهو افتراض المشروع). |
| 24 | **CORS** (بند 15) | لا حاجة لإعدادات CORS إضافية في الوضع الحالي (same-origin). لو انتقلت لبنية منفصلة (الخيار C) ستحتاج CORS + تعديلات كوكيات. |

---

## ماذا أحتاج أن ترسله لي بالضبط (لإغلاق 🟠 والتحقق النهائي)

1. **لقطة إعدادات مشروع Vercel**: Framework Preset, Build Command, Output Directory, Root Directory, Install Command, Node.js Version.
2. **الرابط المباشر** للموقع المنشور.
3. من المتصفح (F12) على الرابط المنشور:
   - تبويب **Console**: نص الأخطاء كاملًا (أتوقع: `TypeError: Cannot read properties of undefined (reading 'map')` من `Toasts`).
   - تبويب **Network**: طلبات `/api/auth/me` و `/api/home` — الأسلوب والاستجابة (أتوقع: `200` من نوع `text/html` أو `404`).
4. **قائمة Environment Variables** المعرّفة على Vercel (الأسماء فقط تكفي).
5. **نص تحذير `esbuild@0.25.12` الحرفي** كما ظهر في سجل Vercel (لتأكيد 🟢 القطعي).
6. هل لديك (أو تخطط لامتلاك) **خادم Node خارجي** (VPS أو Railway/Render/Fly/Heroku)؟ وهل النطاق مرتبط حاليًا بـ Vercel DNS؟

---

## خطة الإصلاح (مرتبة من الأهم إلى الأقل أهمية)

### المرحلة 1 — إصلاح الانهيار (مطلوب في كل الأحوال، 1-2 سطر)
1. **`client/src/store.jsx` سطر 91-92**: إضافة `toasts` إلى كائن `value` وإلى مصفوفة اعتماديات `useMemo`.
   → تنتهي الشاشة البيضاء فورًا (في dev و production على حد سواء).
2. إعادة البناء `npm run build` ثم فتح الموقع: يجب ألا يكون هناك أي خطأ Console عند الإقلاع، وتظهر الواجهة.

### المرحلة 2 — اختيار بنية الاستضافة الصحيحة (قرار واحد يحدد الباقي)
- **الخيار A (موصى به — صفر تغيير كود):** نشر المشروع كله كخادم Node واحد على بيئة تدعم عمليات دائمة + قرص دائم: **Railway / Render / Fly.io / VPS (DigitalOcean, Hetzner…)**.
  الخطوات: `npm install && npm run build && npm start`، ربط النطاق، ضبط المتغيرات (المرحلة 3)، تثبيت Volume على `data/`. Vercel إما يُلغى أو يُبقى كمعاينة واجهة فقط مع إدراك أن APIs لن تعمل هناك.
- **الخيار B (تجنّبه):** إبقاء الواجهة على Vercel ونقل الـ API لخادم Node منفصل بنطاق ثانٍ. يتطلب تعديل كود: نقطة أساس للطلبات في `api.jsx` (متغير بيئة `VITE_API_URL`)، تفعيل CORS في الخادم، تغيير خصائص الكوكيات (SameSite/secure)، ومعالجة WebSocket عبر النطاقات. تكلفة تعقيد أعلى من قيمة الخيار A.
- **الخيار C (غير المجدي):** محاولة تشغيل Express/SQLite/WebSocket داخل Vercel Functions — مرفوض تقنيًا (لا WebSocket دائم، لا قرص دائم).

### المرحلة 3 — متغيرات البيئة على مضيف الإنتاج (لا تعتمد على الافتراضيات)
| المتغير | القيمة المطلوبة |
|---|---|
| `NODE_ENV` | `production` |
| `PORT` | `8080` (أو منصة الاستضافة) |
| `SECRET` | سلسلة عشوائية طويلة (مفتاح التوقيع) |
| `PUBLIC_URL` | `https://نطاقك` (بدون شرطة سفلية نهائية) — مهم لروابط البريد و Socket.IO CORS |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | حساب المدير الأول (قبل أول إقلاع) |
| `MAIL_MODE` / `SMTP_*` | `smtp` مع بيانات SMTP حقيقية (لإيميلات الاستعادة) |
| `MAX_FILE_MB` | حسب رغبتك (يطابق إعداد اللوحة) |
| اختياري | `SESSION_*`, `LOGIN_*`, `DATA_DIR` |

### المرحلة 4 — تثبيت وحماية
- `package.json`: إضافة `"engines": { "node": ">=22.13" }` (شرط `node:sqlite`).
- ضمان **قرص دائم** لمجلد `data/` (SQLite + الملفات المرفوعة) — أو نسخ احتياطي دوري (توجد ميزة تصدير JSON في اللوحة).
- HTTPS: عند وضع المنصة خلف proxy (Railway/Render/VPS+nginx) اضبط TLS خارجيًا — `trust proxy: 1` موجود أصلًا.

### المرحلة 5 — تحسينات غير عاجلة
- ترقية `multer` إلى 2.x (إشعار أمني من npm).
- استبدال `alert()` برسائل toast موحدة في `public.jsx` (UX).
- تنظيف ملفات النموذج الأولي من المستودع (`src/`, `index.html` الجذر, `part*`, `Part6`, `index 2`, `public/editorial-hero.jpg`) لتجنب اللبس — **مع الحفاظ على كل وظائف `client/` و`server/`**.
- إضافة `config.version` لمسار `/api/health` أو حذف قراءته.

### المرحلة 6 — قائمة تحقق ما بعد النشر (إثبات أن الموقع يعمل فعلًا)
1. فتح `https://النطاق/` → تظهر الواجهة بلا أخطاء Console.
2. `F5` على `https://النطاق/admin/users` → تعمل (SPA fallback).
3. Network: `/api/meta` و `/api/home` → `200 application/json`.
4. تسجيل الدخول كأدمن من `/login-admin` → لوحة التحكم تعمل.
5. رفع صورة في مكتبة الوسائط → تظهر عبر `/api/up/...`.
6. تسجيل دخول عميل في تبويب ثانٍ وإرسال إشعار من الأدمن → يصل فوريًا (WebSocket) — وConsole خالٍ من أخطاء الشبكة.

---

### ملاحظة أخيرة (مهمة)
> "Deployment completed" على Vercel يثبت فقط أن **ملفات الواجهة بُنيت ونُشرت**. لقد أثبتُّ لك بأدلة أن الموقع الحالي المنشور (١) ينهار عند أول Render بخطأ `toasts`، و(٢) حتى بعد إصلاحه لا يوجد أي خادم API خلفه. نجاح البناء وسلامة الموقع Runtime **أمران مختلفان تمامًا** — وهذا التقرير يفصل بينهما بالأدلة.
